<IfModule mod_rewrite.c>
    RewriteEngine On

    # Funnel all requests into the public/ web root so the API resolves
    # consistently when the project root is served (e.g. htdocs/SkillSwapApi).
    RewriteCond %{REQUEST_URI} !/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
